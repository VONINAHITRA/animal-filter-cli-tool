/**
 * Represents an animal with a specific name.
 * Each animal has a name attribute.
 */

class Animal {
       constructor(name) {
              this.name = name;
       }
}

module.exports = Animal;
