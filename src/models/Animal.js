/**
 * Represents an animal with a name.
 * @class Animal
 * @param {string} name - The name of the animal.
 */
class Animal {
       constructor(name) {
              this.name = name;
       }
}

module.exports = Animal;
